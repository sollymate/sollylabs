import 'package:flutter/material.dart';

class DashboardNy extends StatelessWidget {
  const DashboardNy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DashboardNy'),
      ),
    );
  }
}
